packages:
  - "apps/*"
  - "packages/*"
  - "netlify"

# Allow native/build scripts only for the packages that genuinely need them.
# (better-sqlite3 dropped in favor of the built-in node:sqlite module.)
onlyBuiltDependencies:
  - esbuild
