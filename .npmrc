# Install devDependencies even when Netlify sets NODE_ENV=production — the web build
# needs vite/typescript (devDeps) to run.
prod=false

# Don't abort the install over peer-dependency mismatches (wallet stack pulls in a
# few); they don't affect the build.
strict-peer-dependencies=false
